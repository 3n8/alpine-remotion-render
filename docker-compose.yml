services:
  remotion-render:
    image: 3n88/alpine-remotion-render:latest
    restart: always
    user: "${PUID}:${PGID}"
    devices:
      - /dev/dri:/dev/dri
    environment:
      - UMASK=${UMASK}
      - TZ=${TZ}
    volumes:
      - ${DOCKER_HOME}/remotion-render:/config
